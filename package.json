{
  "name": "@schuchard/prettier",
  "version": "0.0.0-development",
  "description": "An Angular schematic for adding prettier",
  "scripts": {
    "build": "tsc -p tsconfig.json -w",
    "build:once": "tsc -p tsconfig.json",
    "test": "run-s test:all",
    "test:schematic": "npm run build && jasmine src/**/*_spec.js",
    "test:all": "cd sandbox && yarn && run-s lint test e2e build",
    "test:ci": "cd sandbox && yarn && run-s lint build && cd .. clean",
    "clean": "git checkout HEAD -- sandbox && git clean -f -d sandbox",
    "clean:launch": "run-s clean launch:schematic",
    "launch:schematic": "cd sandbox && ng g @schuchard/prettier:prettier",
    "link:sandbox": "cd sandbox && yarn link \"@schuchard/prettier\"",
    "cm": "git-cz",
    "semantic-release": "semantic-release"
  },
  "keywords": [
    "prettier",
    "schematics",
    "angular"
  ],
  "main": "src/prettier/index.js",
  "author": "Kevin Schuchard",
  "license": "MIT",
  "schematics": "./src/collection.json",
  "dependencies": {
    "@angular-devkit/core": "^0.6.8",
    "@angular-devkit/schematics": "^0.6.8",
    "@types/jasmine": "^2.6.0",
    "@types/node": "^8.0.31",
    "jasmine": "^2.8.0",
    "rxjs": "^6.2.1",
    "typescript": "^2.5.2"
  },
  "devDependencies": {
    "commitizen": "^2.10.1",
    "cz-conventional-changelog": "^2.1.0",
    "npm-run-all": "^4.1.3",
    "semantic-release": "15.5.5"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/schuchard/prettier-schematic.git"
  },
  "publishConfig": {
    "access": "public"
  }
}
